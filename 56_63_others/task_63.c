#include <stdio.h>
#include <stdlib.h>

/*
    #62
    Load user int inputs into two 2D arrays (both 3 rows, 3 columns).
    Display a 2D array interpretting the product 
    of the loaded arrays (matrix times matrix).
*/

int main(){


    return 0;
}